# Runtime Persona Archive

This folder stores superseded runtime persona slices that are not current truth for the live runtime.

Rules:
- active runtime assets live directly under `docs/product/personas/runtime/`
- archived variants such as `*-v1.md` live only under this folder
- loaders and product runtime code must continue to reference the active asset paths, not the archived ones
